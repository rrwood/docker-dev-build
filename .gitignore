# Environment files with secrets
.env
*.env
!.env.example
env.*
!env.example
!portainer.env
user.env

# Claude Code settings and project-specific instructions
.claude/
CLAUDE.md

# LiteLLM config (if you want to keep it local)
.config/

# Workspace data
workspace/

# SSH keys
*.pem
*.key
id_rsa*
authorized_keys

# Docker volumes
*.log

# Push script (local only - not committed to repo)
push.sh
push.ps1
deploy.sh
README_PUSH_SCRIPT.txt

# Generated files
copy-ssh-keys.sh
copy-ssh-keys.ps1

# Docker/Portainer specific (may be customized per user)
# Uncomment if you don't want to version these:
# Dockerfile
# docker-compose.yml

# User-specific files
ngrok.yml

# Local-only files and temporary files
*.local
.DS_Store
Thumbs.db
